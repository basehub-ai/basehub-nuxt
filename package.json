{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "basehub build && nuxt build && basehub build --output ./.output/.basehub",
    "dev": "basehub dev & nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "basehub": "^7.5.28",
    "nuxt": "^3.14.159",
    "vue": "latest",
    "vue-router": "latest"
  }
}
